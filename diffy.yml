server:
  port: 8888

proxy:
  port: 8880

candidate: "localhost:9000"

master:
  primary: "localhost:9000"
  secondary: "localhost:9000"

service:
  protocol : "http"

serviceName : "Diffy Sample Service"

allowHttpSideEffects: "true"


